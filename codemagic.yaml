workflows:
  ios-workflow:
    name: Build & Deploy iOS (TestFlight)
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic TILA   # Le nom exact donné à votre intégration dans Codemagic Settings
    environment:
      node: 20.18.0
      xcode: latest
      cocoapods: default
      ios_signing:
        distribution_type: app_store
        bundle_identifier: ci.tila.app
    scripts:
      - name: Installer les dépendances npm
        script: |
          npm install --legacy-peer-deps

      - name: Préparer le projet Expo (Prebuild)
        script: |
          npx expo prebuild --platform ios --clean

      - name: Installer les Pods CocoaPods
        script: |
          cd ios
          pod install

      - name: Initialiser les profils de signature automatique Apple
        script: |
          app-store-connect fetch-signing-files "ci.tila.app" \
            --type IOS_APP_STORE \
            --create

      - name: Appliquer les profils de signature à Xcode
        script: |
          xcode-project use-profiles

      - name: Compiler le package iOS signé (.IPA)
        script: |
          xcode-project build-ipa \
            --workspace "ios/TILA.xcworkspace" \
            --scheme "TILA"

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
